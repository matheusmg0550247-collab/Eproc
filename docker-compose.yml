services:
  eproc_bastao:
    build: .
    container_name: eproc_bastao
    ports:
      - "8503:8501"
    restart: unless-stopped
    environment:
      - STREAMLIT_SERVER_PORT=8501
      - STREAMLIT_SERVER_ADDRESS=0.0.0.0
